John Kennedy Assignment 1

In order to run my assignment please copy your .pem key in this folder, once unzipped, in order for my script to run sucessfully.

run_newwebserver.py = script you should execute to create a new instance.
logs.py = A script that stores my ec2 class to assist with logging all statements.
check_webserver.py = A script that will be copied to the new instance to monitor it and to install nginx and mysql.

ec2.log = once you run run_newwebserver.py, this file will be created that stores the log statements.

